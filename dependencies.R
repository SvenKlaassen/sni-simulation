# Declare project-wide package dependencies for renv to detect.
# renv scans source files for library() / require() calls to build the lockfile.
# Add packages here as your simulations grow.

library(data.table)
library(ggplot2)
